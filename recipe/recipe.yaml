schema_version: 1

context:
  version: "1.0.0b260730"
  python_min: "3.10"

package:
  name: agent-framework-a2a
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/a/agent-framework-a2a/agent_framework_a2a-${{ version }}.tar.gz
  sha256: a8400751c6e9192508c6965a2d8320fc81bfc992047b5b1d20de97425dd7dd0c

build:
  noarch: python
  number: 0
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - flit-core <4.0,>=3.11
  run:
    - python >=${{ python_min }}
    - agent-framework-core <2,>=1.8.0
    - a2a-sdk <2,>=1.0.0

tests:
  - python:
      imports:
        - agent_framework.a2a
      python_version: ${{ python_min }}.*
      pip_check: true

about:
  summary: |
    A2A integration for Microsoft Agent Framework.
  homepage: https://pypi.org/project/agent-framework-a2a/
  repository: https://github.com/microsoft/agent-framework
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - killua156
    - mgorny
